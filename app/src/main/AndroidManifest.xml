<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.challenge.travel_buddy">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MVVMApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".bus.view.ui.BusPointListActivity"></activity>
        <activity android:name=".bus.view.ui.SearchBusActivity" />
        <activity android:name=".bus.view.ui.SearchActivity" />
        <activity android:name=".bus.view.ui.BusStationList" />
        <activity
            android:name=".train.trainsearch.view.ui.TrainSearch"
            android:screenOrientation="portrait" />
        <activity android:name=".train.view.ui.StationListActivity" />
        <activity android:name=".train.view.ui.SearchActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>